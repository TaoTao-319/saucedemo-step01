# 测试执行记录说明

本次项目共执行 19 条用例，结果为 16 条 Pass、3 条 Fail、0 条 Blocked。Excel 是执行记录主表，Word 是公司内部报告，Markdown 是 GitHub 浏览版本。

## 每条用例执行后填写

1. 实际操作是否与步骤一致。
2. 页面实际发生了什么。
3. 实际结果是否符合预期结果。
4. 标记 `Pass`、`Fail` 或 `Blocked`。
5. 如果是 `Fail`，保存截图并创建对应缺陷报告。

## 缺陷成立条件

缺陷必须满足：

- 能够稳定复现；
- 复现步骤足够清楚；
- 实际结果和预期结果不同；
- 有截图、录屏或其他证据；
- 已填写严重程度和优先级。

如果问题无法稳定复现，先记录为观察项，不要直接提交为正式缺陷。

## 当前缺陷

- BUG-001 对应 TC-006，证据文件为 `BUG-001.png`。
- BUG-002 对应 TC-007，证据文件为 `BUG-002.png`。
- BUG-003 对应 TC-015，证据文件为 `BUG-003.png`。

## 严重程度和优先级

- 严重程度：问题对系统功能或用户的影响有多大。
- 优先级：问题应该多快被修复。

例如：无法提交订单通常是 High 或 Critical 严重程度、High 优先级；商品图片轻微显示问题通常是 Low 或 Medium 严重程度。
